spring:
  application:
    name: codeeat

server:
  port: 3502

llm:
  provider: ${LLM_PROVIDER:openai}
  api-url: ${LLM_API_URL:}
  api-key: ${LLM_API_KEY:${GROQ_API_KEY:}}
  model: ${LLM_MODEL:gpt-4o-mini}
